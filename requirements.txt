# --- Keep-Alive / Servidor Web ---
flask>=3.0.0 # Para criar o servidor web fake (keep_alive.py) para o Render/UptimeRobot

# --- Processamento de Linguagem Natural (Texto e Áudio) ---
number-parser>=0.3.2 # Para converter números por extenso (ex: "dois", "cinco") em dígitos ("2", "5")
google-generativeai>=0.8.5 # Biblioteca da API do Google Gemini (para chat geral e extração de lembretes)

# --- Bot Telegram ---
python-telegram-bot[job-queue]>=20.0 # A biblioteca principal do Telegram (PTB) com o JobQueue (agendador)
pytz>=2024.0 # Para lidar com fusos horários (timezone) de forma confiável

# Para variáveis de ambiente
python-dotenv>=1.0.0

# --- Banco de Dados (PostgreSQL) ---
SQLAlchemy>=2.0 # Biblioteca (ORM) para interagir com o banco de dados usando Python
psycopg2-binary>=2.9.9 # Driver necessário para o SQLAlchemy "conversar" com o PostgreSQL
